To install all plugins:
-----------------------
1. Set up vundle, run "setup_vundle" script
2. vim ~/.vimrc 
   => do :PluginInstall

To use YouCompleteMe (Code completer):
--------------------------------------
1. Installation with C-family language support
   > cd ~/.vim/bundle/YouCompleteMe
   > ./install.py --clang-completer
   The above commands will do full installation
2. Keep the default options.
3. Copy the following file to the top level of a project folder
   > cp ~/.vim/bundle/YouCompleteMe/third_party/ycmd/cpp/ycm/.ycm_extra_conf.py .
4. Modify the flags to meet the requirements of the project
   Or use a predefined one if suited for the project
   For middleware:
   >  /path/to/freezer/Users/jbyun/vim_stuff/.ycm_extra_conf-middleware.py /path/to/middleware

